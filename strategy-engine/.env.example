# ===================================================
# Strategy Engine — Environment Variables
# ===================================================
# Copy this to .env and fill in the values.

# --- App ---
DEBUG=true

# --- LLM ---
OPENAI_API_KEY=sk-your-openai-key-here
DEEPSEEK_API_KEY=sk-your-deepseek-key-here

# --- Database (PostgreSQL) ---
DATABASE_URL=postgresql+asyncpg://strategy:strategy@localhost:5432/strategy_engine

# --- Vector DB (Qdrant) ---
QDRANT_URL=http://localhost:6333

# --- SEO API (DataForSEO) ---
# DATAFORSEO_LOGIN=your-login
# DATAFORSEO_PASSWORD=your-password

# --- Production System Webhook ---
# The URL of the existing vdo-content system's ingest endpoint.
# This is the ONE-WAY bridge between the two systems.
# PRODUCTION_WEBHOOK_URL=https://vdo-content-xxxx.run.app/api/strategy/ingest
# PRODUCTION_WEBHOOK_TOKEN=your-shared-secret-token

# --- CORS (comma-separated origins) ---
# CORS_ORIGINS=["http://localhost:3000"]
